{
  "schema": "vertical-overrides v1",
  "description": "Per-beat headline and lede overrides for the shell. Edit via /builder.html (admin only). Merges over VERTICALS map on shell boot.",
  "overrides": {}
}
