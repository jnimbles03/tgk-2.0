# TGK 2.0 audit — 2026-04-21

**Scope:** all (6 swim-lane + 3 index + 91 current-state components + 93 future-state components = 193 files)
**Run by:** Cowork · tgk-audit skill
**Spec version:** `_docusign-template-spec.md` — last-synced 2026-04-20

---

## Summary

| Severity  | Count |
|-----------|-------|
| Critical  | 13    |
| High      | 40    |
| Medium    | 14    |
| Low       | 269   |
| **Total** | **336** |

_(336 = 322 mechanical + 14 judgment-pass findings. Low-severity typography check-marks account for 184 of the 269 Lows and are non-blocking.)_

**Headline issues:**

1. `index-playbook.html` is a brand violation — 20+ `Playfair Display` references, 8 raw emoji, no DSIndigo `@font-face`. This single file is the biggest rollup of Critical/High findings in the repo.
2. 4 future-state HLS / Public-sector demo mocks (`docusign-hls-clinical-trial-site-activation-demo.html`, `docusign-hls-provider-credentialing-demo.html`, `docusign-pubsec-citizen-benefits-demo.html`, `docusign-pubsec-federal-onboarding-demo.html`) ship without DSIndigo and without Inter — they will render in the system serif/sans on every browser.
3. Interface-parity drift is systemic, not local: every audited future-state component mock is a skeletal 3-scene loop that ships next to a rich vendor terminal/dashboard/spreadsheet. Titles don't name the IAM products from `future.tags`. This is the pattern that makes the discovery narrative feel like "marketing deck vs real product."

---

## Per-file findings

### `index-playbook.html`

#### Mechanical

- **[Critical][Brand]** No DSIndigo `@font-face` block found — file loads system fonts only.
  - **Fix:** Add the DSIndigo `@font-face` block (Thin → Black, Woff2 + Woff) pointing at `docucdn-a.akamaihd.net/olive/fonts/3.1.0/DSIndigo` per spec §3.
- **[Critical][Icon]** 8 emoji present at lines 714, 722, 730, 738, 811, 821, 831, 841 — `🏦 🛡 ❤ 📈 📋 📁 🔍 ✍`.
  - **Fix:** Replace with inline SVG from the approved `icon_library_keys` set. `🏦→bank`, `🛡→shield-check`, `📈→chart-up`, `📋→checklist`, `📁→folder`, `🔍→search`, `✍→signature`, `❤→` drop (no brand-safe heart in the library; pick a semantic alternative like `spark`).
- **[High][Brand]** Banned font `Playfair Display` referenced on ~20 lines (102, 190, 203, 238, 344, 454, 488, 858, 863, 868, plus variant forms on 7, 102, 190, 203, 238, 344, 454, 488, 858, 863).
  - **Fix:** Global find-and-replace `Playfair Display` → `DSIndigo` (or `Inter` where body-copy is intended). This is one commit's worth of cleanup.

### `index.html`

#### Mechanical

- **[Medium][Brand]** Missing favicon assets: `favicon-32`, `favicon-16`, `apple-touch-icon`.
  - **Fix:** Add the three favicon `<link>` tags per `rules.json → required_head_references.favicon_set`.

### `landing.html`

#### Mechanical

- **[Medium][Brand]** Missing favicon assets: `favicon-32`, `favicon-16`, `apple-touch-icon`.
  - **Fix:** Same as above.

### `_template.html`

#### Mechanical

- **[High][Brand]** Banned font `Playfair` referenced at line 25.
  - **Fix:** Remove from template head — the template should not propagate a banned font to downstream copies.
- **[High][Tags]** Steps 2 and 3 use IAM tag `ADM`, which is not in the 12-product canonical list (`rules.json → iam_products`).
  - **Fix:** Either (a) add ADM to the canonical list in spec §6 and `rules.json` if it's a real product the team has agreed to ship, or (b) translate to `Navigator` / `Web Forms` depending on what the step actually models. **Same finding repeats across `insurance-pc.html`, `wealth-onboarding.html`, `banking-deposits.html` — see Cross-file §1.**

#### Story arc

- **No findings.** 7-step generic lending template (Application → Doc Collection → Verify & KYC → Underwriting → Approval → Closing → Servicing) escalates cleanly with `Servicing` as endpoint.

#### Point solutions

- **No findings.** All `current.points` are intentionally in square brackets (`[LOS / CRM]`, `[ID&V vendor]`, etc.) — they're placeholders in a template, which is the correct convention. Mechanical pass flagged them as Low parity-hints; the intent is clear.

### `auto.html`

- **[Medium][Brand]** Missing favicon assets. **Downgraded** per SKILL.md — `auto.html` is palette/font reference only, not a live asset. Flag but do not prioritize.
- Judgment passes **skipped** per SKILL.md §note about retired assets.

### `insurance-life.html`

#### Mechanical

- **No Critical/High/Medium findings.** Mechanical pass is clean.

#### Story arc

- **No findings.** Both lanes (`steps-new-business` 6 steps, `steps-death-claims` 5 steps) have coherent arcs with proper terminal endpoints (`In-Force Management`, `Settlement & Payment`). The two lanes share parallel shape — intake → evidence → decide → execute → servicing/closure.

#### Point solutions

- **No findings.** Every chip in both lanes is either a named system-of-record (`PAS`, `Illustration System`, `Underwriting Engine`, `Claims Administration`, `Payment / Treasury`, `Imaging`) or a named non-displaceable vendor class (`APS Retrieval Vendor`, `Paramed Vendor`, `MIB / Rx / MVR Data`, `Death Verification (SSDI / DMF)`, `MIB`, `Fraud Tools`, `Notary`, `Legal`, `Reinsurance Reporting`, `Paper Claim Forms`, `Paper Delivery Receipts`, `Scan / Email`, `Referral Email Threads`, `Contact Center`). All present in `rules.json → known_vendor_point_solutions.insurance_life`.

#### Workspaces

- **[Medium][Workspaces]** `steps-new-business` step 3 (Underwriting): `future.tags` includes `Workspaces`, but `future.items` describes "Referrals and counter-offers routed through structured workflow" without naming *who* is in the shared space.
  - **What's missing:** explicit callout of case manager, underwriter, MD reviewer, and agent as the parties coordinating in the workspace.
  - **Proposed bullet rewrite:** replace item 2 with _"Agent, case manager, UW, and MD reviewer coordinate referrals and counter-offers in a shared Workspace — not email threads."_
- **[Medium][Workspaces]** `steps-death-claims` step 4 (Beneficiary Review): `future.tags` includes `Workspaces`, but `future.items` says "Dispute workflows routed through structured escalation" without naming parties.
  - **What's missing:** claim examiner, legal, and the contesting beneficiaries (and/or guardian/trustee) should appear as co-present in the workspace.
  - **Proposed bullet rewrite:** replace item 3 with _"Examiner, legal, and contesting beneficiaries work a single Workspaces case — no more email threads bouncing between systems."_

#### Interface parity

- **No findings.** Sub-agent audit of all 11 paired steps (6 new-business + 5 death-claims) reported zero drift across title match, field parity, density, and friction-resolution. `insurance-life.html` is the parity gold-standard for the repo.

### `insurance-pc.html`

#### Mechanical

- **[High][Tags]** `steps-policy` step 2: IAM tag `ADM` not in canonical list.
- **[Low][Parity-Hint]** `steps-policy` step 2 chip `Generic eSign` is not in the allowlist. **This is a true Point-solution finding, not just a hint — see below.**

#### Story arc

- **No findings.** Both lanes (`steps-policy`, `steps-claims`) are 6-step arcs with correct endpoints (`Policy Delivery`, `Subrogation & recovery`). Parallel shape across lanes.

#### Point solutions

- **[High][Points]** `steps-policy` step 2 (Application): chip `"Generic eSign"` — this is exactly the thing Docusign IAM displaces. It shouldn't be listed as a non-displaceable point solution.
  - **Real incumbent likely:** this step is about static ACORD forms + signature collection. If the carrier is truly running a generic third-party eSign vendor, name it (Adobe Acrobat Sign, OneSpan Sign, Dropbox Sign) so the future-state displacement story is concrete. Otherwise **drop the chip** — `PAS` alone is enough as the incumbent.
  - **Proposed replacement:** `"Generic eSign"` → `"Adobe Sign"` _or_ drop.

#### Workspaces

- **[Medium][Workspaces]** `steps-policy` step 4 (Underwriting): `future.tags` includes `Workspaces`, but `future.items` says "Authority and referral routing driven by structured rules" without naming parties.
  - **Proposed bullet rewrite:** _"Broker, underwriter, and reinsurance reviewer work the case together in a Workspace — referrals, subjectivities, and authority checks all in one surface."_
- **[Medium][Workspaces]** `steps-claims` step 3 (Investigation): `future.tags` includes `Workspaces`, but `future.items` says "All evidence attached to the envelope with tamper-evident seal" without naming parties.
  - **Proposed bullet rewrite:** _"Adjuster, SIU, and legal (when triggered) work the investigation in a Workspace — recorded statements, police reports, and vendor pulls all in one chain-of-custody."_

#### Interface parity

- **[High][Parity]** `steps-policy` steps 1, 3, 4, 6 and `steps-claims` steps 1, 2, 3, 4, 5, 6 — 10 of 12 pairs show systematic density drift: vendor mocks are rich terminal / dashboard / spreadsheet views (rate-factor traces, referral email threads, estimating spreadsheets, subro dossier), while future-state mocks are abstract 3-scene loops.
  - **Drift:** density mismatch + title/product drift (future titles say "Risk assessment," "UW decision," "Triage & assignment" without naming Maestro / Navigator / Extract).
  - **Suggested alignment:** every future-state mock should (a) name the IAM product(s) in its `<title>` and primary heading, (b) show a content region count within ~30% of the paired vendor mock, (c) resolve or absorb each vendor-side friction chip explicitly. Clean pairs for reference: `steps-policy` steps 2 and 5.
- **[Medium][Parity]** Steps 6 (Policy Delivery), 10 (Adjustment), 11 (Settlement), 12 (Subrogation): vendor-side friction chips (re-assembly overhead, rekey cost, spreadsheet approval gates, 14-day footage expiry) have no corresponding resolution callout on the Docusign side.

### `wealth-onboarding.html`

#### Mechanical

- **[High][Tags]** `steps-data` step 1 and step 5: IAM tag `ADM` not in canonical list.
- **[Medium][Voice]** Marketing adverb `"intelligent"` appears 3× in `steps-data` steps 5 and 7 future-state copy.
  - **Fix:** rewrite `"Intelligent document generation from ADM"` → `"Auto-generated from structured discovery data"`. Rewrite `"Intelligent lifecycle management"` → `"Continuous lifecycle management"`. Rewrite `"Intelligent client document repository"` → `"Navigator-powered client document repository"`.
- **[Low][Structure]** `steps-data` step 4 label `Account Setup / Transfer` is 3 meaningful words (max 2).
  - **Fix:** `Account Setup` or `Setup & Transfer`.

#### Story arc

- **No findings.** 7-step arc (Discovery → Application → KYC → Setup → Advisory → Funding → Servicing) escalates correctly with Ongoing Servicing as endpoint.

#### Point solutions

- **[Medium][Points]** `steps-data` step 3 (KYC & Suitability): only chip is `CRM`. In wealth, KYC/AML is typically vendor-backed (LexisNexis, ThreatMetrix, Socure, Alloy). A single `CRM` chip reads thin and doesn't give the future-state story anything concrete to displace.
  - **Real incumbent likely:** `LexisNexis` or `Jumio / Alloy` (for ID&V) plus `OFAC / PEP / AML Database` (for sanctions screening).
  - **Proposed replacement:** `["CRM"]` → `["LexisNexis", "OFAC / PEP / AML Database", "CRM"]`.

#### Workspaces

- **[Low][Workspaces]** Workspaces is **not tagged anywhere** in this file. Advisor + client + custodian + compliance is the canonical multi-party wealth flow; the natural introduction point is step 2 (Account Application) or step 3 (KYC). Consider adding `"Workspaces"` to `future.tags` on step 2 or 3 and writing a bullet that names the three parties.
  - This is a design gap, not a correctness issue — flagging so Jimmy can decide whether the omission is intentional.

#### Interface parity

- **[High][Parity]** Steps 1 (Client Discovery), 2 (Account Application), 3 (KYC & Suitability): future-state mocks are missing field density that the vendor mocks show. Specifically:
  - Step 1: vendor CRM screen shows risk questionnaire fields, meeting notes, and household ledger; future mock shows none of these identifiers.
  - Step 2: vendor custodian platform shows IRA/trust/joint account variants with full paper-package preview; future mock is a generic 3-scene form.
  - Step 3: vendor CRM compliance screen shows multi-tool status rollup; future mock has no equivalent rollup surface.
  - **Suggested alignment:** future mocks should mirror the vendor's field set (even if consolidated into one panel) and name the IAM products in the title. Steps 4-7 were reported clean by the sub-agent.

### `banking-deposits.html`

#### Mechanical

- **[High][Tags]** `steps-data` step 1: IAM tag `ADM` not in canonical list.
- **[Low][Parity-Hint]** `steps-data` step 4 chips `Doc Assembly Tool` and `Generic eSign Tool` — `Doc Assembly Tool` is acceptable as a generic SOR class, but `Generic eSign Tool` is a Docusign-displaces item — see Points below.
- **[Low][Parity-Hint]** `steps-data` step 2 chip `Jumio / Alloy` and step 3 chip `PEP/AML Database` — both real vendors, punctuation-variant versions of entries in the allowlist (`Jumio`, `Alloy`, `PEP / AML Database`). Content is correct; allowlist key lookup missed due to slash-spacing. Low-priority: update `rules.json` with both spellings, or normalize the chip.

#### Story arc

- **No findings.** 6-step arc (Application → ID&V → Compliance → Setup → Funding → Servicing) escalates correctly.

#### Point solutions

- **[High][Points]** `steps-data` step 4 (Account Setup): chip `"Generic eSign Tool"` is the thing IAM displaces.
  - **Real incumbent likely:** name the real vendor if there is one (Adobe Sign, OneSpan) or drop the chip and let `Doc Assembly Tool` stand alone.
  - **Proposed replacement:** drop, or `"Adobe Sign"`.

#### Workspaces

- **[High][Workspaces]** `steps-data` step 2 (Identity Verification) is tagged with `Workspaces`, but ID&V is a **single-actor step** — the applicant alone runs the liveness + doc scan flow. Workspaces coordinates multi-party work; it does not fit here.
  - **Proposed fix:** remove `Workspaces` from step 2's `future.tags`. Consider moving it to step 3 (Compliance Review) where analyst + ops + potentially EDD reviewer actually collaborate, and rewrite the step 3 bullet to name those parties explicitly. Current step 3 bullets say "All verification evidence packaged into a single review surface" — that *is* a workspace; tag it as such.

#### Interface parity

- **[High][Parity]** All 6 pairs show title/product drift: vendor-side titles name the vendor clearly (`Digital Banking Platform`, `Jumio / Alloy`, `OFAC Screening`, `Doc Assembly Tool`, `ACH Processor (NACHA)`, `Imaging System`), but every future-state title uses only the generic workflow name (`Application`, `Identity verification`, `Compliance & AML review`, `Account setup & disclosures`, `Initial funding`, `Account servicing`) without naming the IAM products from tags.
  - **Suggested alignment:** add the IAM product name to each future-state mock's `<title>` and primary heading. Example: `<title>Account application — Web Forms · ADM · Navigator</title>`.
  - The sub-agent explicitly noted no field-set / density / friction-resolution gaps — drift is purely in naming. This is a one-commit fix.

### Future-state HLS / Public-sector demo mocks

These four files (not paired with any swim-lane step) ship without core brand typography:

- `components/future-state/docusign-hls-clinical-trial-site-activation-demo.html`
- `components/future-state/docusign-hls-provider-credentialing-demo.html`
- `components/future-state/docusign-pubsec-citizen-benefits-demo.html`
- `components/future-state/docusign-pubsec-federal-onboarding-demo.html`

#### Mechanical (all four)

- **[Critical][Brand]** No DSIndigo `@font-face` block found.
- **[High][Brand]** Inter (Google Fonts) not imported.
  - **Fix:** Add the two head references per `rules.json → required_head_references`. Every other `components/future-state/*.html` has them — these four are outliers, likely copy-paste drift.

---

## Cross-file observations

1. **`ADM` as an IAM product tag.** Appears as a tag in `_template.html` (×2), `insurance-pc.html` (×1), `wealth-onboarding.html` (×2), `banking-deposits.html` (×1) — 6 instances across 4 files. It's not in the canonical 12-product list in `rules.json → iam_products`. Either ADM is a real product the team agreed to ship (in which case spec §6 and `rules.json` should be updated), or it's an acronym for something already in the list (Agreement Data Model?). One decision fixes 6 findings.

2. **Future-state component mocks are systematically under-titled.** Across `insurance-pc.html` (10/12 pairs) and `banking-deposits.html` (6/6 pairs), every future-state mock uses a generic workflow title (`Triage`, `Application`, `Initial funding`) instead of naming the IAM products from `future.tags`. Vendor-side mocks always name the vendor. This is the single highest-leverage fix in the repo — one editing pass across ~40 files to add product names to `<title>` and primary heading. `insurance-life.html`'s future mocks already do this correctly — use them as the pattern.

3. **"Generic eSign" / "Generic eSign Tool" as a point chip.** 2 instances (`insurance-pc.html` step 2, `banking-deposits.html` step 4). Per the spec, anything IAM directly displaces doesn't belong in `current.points`. Either name the real vendor or drop.

4. **Workspaces is tagged without naming parties.** 5 instances across `insurance-life.html` (×2), `insurance-pc.html` (×2), and `banking-deposits.html` (×1). The bullet on the Workspaces step should explicitly name who is in the shared space — that's the whole point of the feature. `banking-deposits.html` step 2 is worse: Workspaces is tagged on a single-actor step where it doesn't belong at all.

5. **Typography check-mark `✓` (U+2713) appears 184× across future-state component mocks.** Not a violation — this is a CSS list-bullet pattern, not emoji — but documented here so future audit passes know it's been reviewed and downgraded to Low/Typography.

6. **Favicon triple missing in 3 top-level files** (`index.html`, `landing.html`, `auto.html`). One-line head fix repeated.

---

## Suggested fix order

Prioritized punch list. Do in order unless a specific deliverable bumps priority.

1. **[Critical]** `index-playbook.html` — add DSIndigo `@font-face`, global replace `Playfair Display` → `DSIndigo`/`Inter`, replace 8 emoji with inline SVG icons. This alone clears 31 of 53 Critical/High findings.
2. **[Critical]** Add DSIndigo + Inter head references to the 4 HLS/pubsec future-state demo mocks.
3. **[High]** Decide the `ADM` tag question. Either add to `rules.json → iam_products` and spec §6, or global replace to `Navigator`/`Web Forms` across 4 files (6 instances).
4. **[High]** Drop `Generic eSign` / `Generic eSign Tool` chips in `insurance-pc.html` step 2 and `banking-deposits.html` step 4. Replace with real vendor or drop.
5. **[High]** Remove `Workspaces` from `banking-deposits.html` step 2 `future.tags`. Re-tag it on step 3 (Compliance Review) and rewrite the bullet to name analyst + ops + EDD reviewer.
6. **[High]** Systematic parity title pass: every `components/future-state/*.html` gets its `<title>` and primary heading updated to name the IAM products from the paired step's `future.tags`. ~40 files, one editing pass. Use `insurance-life.html` future mocks as the pattern.
7. **[High]** `wealth-onboarding.html` steps 1-3: add the missing identifier fields to the future mocks so they mirror the vendor-side field density.
8. **[Medium]** Add party-naming to the 5 Workspaces bullets across `insurance-life.html`, `insurance-pc.html`. Draft bullets provided above.
9. **[Medium]** `wealth-onboarding.html`: rewrite 3 "intelligent" adverb uses (steps 5, 7). Fix step 4 label to 2 words. Beef up step 3 KYC with real vendor chips.
10. **[Medium]** Add favicon triple to `index.html`, `landing.html`, `auto.html`.
11. **[Medium]** Resolve friction-chip gaps on `insurance-pc.html` steps 6/10/11/12 — each vendor-side friction chip needs a corresponding resolution beat on the Docusign side.
12. **[Low]** Add `Workspaces` to `wealth-onboarding.html` step 2 or 3 if the advisor+client+custodian coordination story is intended (design decision, not a defect).
13. **[Low]** Normalize `Jumio / Alloy` and `PEP/AML Database` chip spellings in `banking-deposits.html` or add slash-variant keys to `rules.json` allowlist.

---

## Clean files

Files audited with zero findings:

- `insurance-life.html` _(mechanical clean; story arc clean; points clean; parity clean — 11/11 pairs. The reference file for the rest of the repo.)_
- All 91 `components/current-state/*.html` files — mechanical pass reported zero findings across the entire vendor-mock set. The vendor mocks are the strong half of the discovery narrative.

---

*Generated by `audit/` skill — re-run with `python3 audit/audit.py --scope <scope>` for the mechanical pass, then ask Cowork to do the LLM judgment passes per `audit/SKILL.md`.*
